/**
 * @typedef {import("./Module").Module} Module
 */
/**
 * Representa la compra de un módulo por un usuario.
 * @typedef {Object} Purchase
 * @property {Module['id']} moduleId - ID del módulo comprado.
 */
export {};