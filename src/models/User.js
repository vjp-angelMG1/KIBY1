/**
 * Representa un usuario del sistema.
 * @typedef {Object} User
 * @property {string} email - El correo electrónico del usuario.
 * @property {string} uid - El ID único de autenticación.
 * @property {string} [role] - El rol del usuario (ej. 'admin', 'user').
 * @property {Array<string>} [purchases] - Array de IDs de los módulos comprados.
 */
export {};